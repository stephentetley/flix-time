[package]
name        = "flix-time"
description = "A project to wrap Java's time library so it can be used by Flix."
version     = "0.4.0"
flix        = "0.37.0"
license     = "Apache-2.0"
authors     = ["Stephen Tetley <stephen.tetley@gmail.com>"]

[dependencies]
"github:stephentetley/graded-lib"                = "0.6.1"
"github:stephentetley/interop-base"              = "0.6.0"
