# flix-time

A project to wrap Java's time library so it can be used by Flix.

License: Apache 2.0

News - version 0.16.0 - namespace changes modules are now in the 
namespace `InteropTime`. Types have been pushed down one layer 
into their respective modules.
